package sample;

import java.util.Observable;
import java.util.Observer;

public class View {
 /*   Model m;

    public View(Model m){
        this.m = m;
    }

    @Override
    public void update(Observable o, Object arg) {
        m.getLayout().getChildren().clear();
        m.getLayout().getChildren().add(m.getBox());
    }

  */
}
